# Generated by scripts/package-manifests.ts. Do not edit by hand.
class Looptroop < Formula
  desc "Local AI coding orchestration with council planning and human-gated PRs"
  homepage "https://www.looptroop.ovh/"
  url "https://github.com/looptroop-ai/LoopTroop/releases/download/v0.5.6/looptroop-0.5.6-bundle.tar.gz"
  sha256 "39ca1e1a9cb6920e40e837f1ff673161b5b78542cd1568a467a75ed0a6493b1f"
  license "MIT"

  livecheck do
    url :stable
    strategy :github_latest
  end

  depends_on "gh"
  depends_on "node@24"
  uses_from_macos "git"

  def install
    libexec.install Dir["*"]
    (libexec/".install-channel").write "homebrew"
    (bin/"looptroop").write_env_script libexec/"bin/looptroop",
                                       PATH: "#{formula_opt_bin("node@24")}:$PATH"
  end

  def caveats
    <<~EOS
      LoopTroop drives OpenCode, which it does not install. Run:
        looptroop doctor
    EOS
  end

  test do
    assert_match version.to_s, shell_output("#{bin}/looptroop --version")
  end
end
